#include <stdio.h>

int main() {
    int a = 10, b = 5;
    int resultado;
    // Reemplaza el operador en la siguiente línea
    resultado = (a != b);
    printf("El resultado es %d\n", resultado);
    return 0;
}